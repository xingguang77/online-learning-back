<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.usst.mapper.AdTeacherMapper">

    <select id="list" resultType="cn.usst.pojo.dto.TeacherDTO">
        SELECT
        t.id,
        t.user_id AS userId,
        u.username,
        u.name,
        t.title,
        t.introduction
        FROM teacher t
        LEFT JOIN user u ON t.user_id = u.id
        <where>
            <if test="name != null and name != ''">
                AND u.name LIKE concat('%', #{name}, '%')
            </if>
        </where>
        ORDER BY t.id DESC
    </select>

    <insert id="insert">
        INSERT INTO teacher(user_id, title, introduction)
        VALUES (#{userId}, #{title}, #{introduction})
    </insert>

    <update id="update">
        UPDATE teacher
        SET title = #{title}, introduction = #{introduction}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM teacher WHERE id = #{id}
    </delete>

</mapper>