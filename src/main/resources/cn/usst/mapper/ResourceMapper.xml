<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.usst.mapper.ResourceMapper">

    <resultMap id="ResourceDTOMap" type="cn.usst.pojo.dto.ResourceDTO">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="courseId" column="course_id"/>
        <result property="classId" column="class_id"/>
        <result property="userId" column="user_id"/>
        <result property="uploaderRole" column="uploader_role"/>
        <result property="visibility" column="visibility"/>
        <result property="status" column="status"/>
        <result property="downloadCount" column="download_count"/>
        <result property="createTime" column="create_time"/>
        <result property="uploaderName" column="uploaderName"/>
        <result property="courseName" column="courseName"/>
        <collection property="fileUrls" ofType="java.lang.String">
            <result column="file_url"/>
        </collection>
    </resultMap>

    <select id="selectResourcesForStudent" resultMap="ResourceDTOMap">
        SELECT
        r.*,
        u.name as uploaderName,
        c.name as courseName,
        rf.file_url
        FROM resource r
        LEFT JOIN user u ON r.user_id = u.id
        LEFT JOIN course c ON r.course_id = c.id
        LEFT JOIN resource_file rf ON r.id = rf.resource_id
        WHERE r.status = 1
        AND (
        r.visibility = 1
        OR
        (r.visibility = 0 AND r.class_id IN (
        SELECT class_id FROM student_class WHERE student_id = #{studentId}
        ))
        )
        <if test="dto.keyword != null and dto.keyword != ''">
            AND (
            r.title LIKE concat('%', #{dto.keyword}, '%')
            OR
            r.description LIKE concat('%', #{dto.keyword}, '%')
            )
        </if>
        <if test="dto.courseId != null">
            AND r.course_id = #{dto.courseId}
        </if>
        ORDER BY r.create_time DESC
    </select>

    <select id="selectResourcesForAdmin" resultMap="ResourceDTOMap">
        SELECT
        r.*,
        u.name as uploaderName,
        c.name as courseName,
        rf.file_url
        FROM resource r
        LEFT JOIN user u ON r.user_id = u.id
        LEFT JOIN course c ON r.course_id = c.id
        LEFT JOIN resource_file rf ON r.id = rf.resource_id
        WHERE 1=1
        AND r.status = 1
        <if test="dto.keyword != null and dto.keyword != ''">
            AND (
            r.title LIKE concat('%', #{dto.keyword}, '%')
            OR
            r.description LIKE concat('%', #{dto.keyword}, '%')
            )
        </if>
        <if test="dto.courseId != null">
            AND r.course_id = #{dto.courseId}
        </if>
        ORDER BY r.create_time DESC
    </select>

    <select id="selectResourceById" resultMap="ResourceDTOMap">
        SELECT r.*, u.name as uploaderName, c.name as courseName, rf.file_url
        FROM resource r
                 LEFT JOIN user u ON r.user_id = u.id
                 LEFT JOIN course c ON r.course_id = c.id
                 LEFT JOIN resource_file rf ON r.id = rf.resource_id
        WHERE r.id = #{id} AND r.status = 1
    </select>

    <insert id="insertResource" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO resource(title, description, course_id, class_id, user_id, uploader_role, visibility, status, download_count, create_time)
        VALUES (#{title}, #{description}, #{courseId}, #{classId}, #{userId}, #{uploaderRole}, #{visibility}, #{status}, 0, NOW())
    </insert>

    <insert id="insertResourceFiles">
        INSERT INTO resource_file(resource_id, file_url, file_type) VALUES
        <foreach collection="urls" item="url" separator=",">
            (#{resourceId}, #{url}, 'attachment')
        </foreach>
    </insert>

    <select id="checkStudentClass" resultType="java.lang.Integer">
        SELECT count(*) FROM student_class WHERE student_id = #{studentId} AND class_id = #{classId}
    </select>

    <update id="updateResourceContent">
        UPDATE resource
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectStudentClasses" resultType="cn.usst.pojo.dto.ClazzVO">
        SELECT
            c.id,
            c.class_name,
            c.course_id,
            co.name AS course_name
        FROM student_class sc
                 JOIN class c ON sc.class_id = c.id
                 JOIN course co ON c.course_id = co.id
        WHERE sc.student_id = #{studentId}
    </select>

    <select id="selectMyResources" resultMap="ResourceDTOMap">
        SELECT r.*, c.name as courseName, rf.file_url
        FROM resource r
        LEFT JOIN course c ON r.course_id = c.id
        LEFT JOIN resource_file rf ON r.id = rf.resource_id
        WHERE r.user_id = #{studentId} AND r.status = 1
        <if test="dto.keyword != null and dto.keyword != ''">
            AND r.title LIKE concat('%', #{dto.keyword}, '%')
        </if>
        ORDER BY r.create_time DESC
    </select>

    <select id="selectOwnerId" resultType="java.lang.Long">
        SELECT user_id FROM resource WHERE id = #{id}
    </select>

    <update id="updateResourceBase">
        UPDATE resource
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="visibility != null">
                visibility = #{visibility}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteResourceFiles">
        DELETE FROM resource_file WHERE resource_id = #{resourceId}
    </delete>


    <select id="selectClassById" resultType="cn.usst.pojo.dto.ClazzVO">
        SELECT
            c.id,
            c.class_name,
            c.course_id,
            co.name AS course_name,    u.name AS teacherName      FROM class c
                                                                           LEFT JOIN course co ON c.course_id = co.id
                                                                           LEFT JOIN user u ON c.teacher_id = u.id
        WHERE c.id = #{id}
    </select>

</mapper>