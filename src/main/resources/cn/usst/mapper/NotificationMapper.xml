<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.usst.mapper.NotificationMapper">
    <resultMap id="BaseResultMap" type="cn.usst.pojo.Notification">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="isRead" column="is_read"/>
        <result property="createTime" column="create_time"/>
        <result property="relatedId" column="related_id"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO notification(user_id, content, type, is_read, create_time, related_id)
        VALUES (#{userId}, #{content}, #{type}, 0, NOW(), #{relatedId})
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM notification WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

</mapper>